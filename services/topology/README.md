# topology

**Cohort:** Spring Boot
**Owned datastore:** Apache AGE (topology graph)

Sole owner of the Apache AGE topology graph; versioned snapshots; query API; emits topology.changed.

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/topology`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

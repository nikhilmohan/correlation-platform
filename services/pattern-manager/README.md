# pattern-manager

**Cohort:** Spring Boot
**Owned datastore:** PostgreSQL (Pattern Store)

Pattern Store + lifecycle, RCA, codebook reconciliation, explainability, human-approval workflow.

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/pattern-manager`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

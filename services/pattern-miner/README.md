# pattern-miner

**Cohort:** Python (PySpark)
**Owned datastore:** — (stateless)

PrefixSpan mining only (Spark MLlib); emits raw mined sequences. Container-only (Spark).

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/pattern-miner`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

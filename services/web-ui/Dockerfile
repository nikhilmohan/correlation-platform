# syntax=docker/dockerfile:1
# TBD: minimal scaffold. Fill in once the Angular app exists (kickoff build stage).
FROM node:24 AS build
WORKDIR /app
# COPY package*.json ./
# RUN npm ci
# COPY . .
# RUN npm run build

FROM nginx:stable-alpine
# COPY --from=build /app/dist/ /usr/share/nginx/html/
# EXPOSE 80

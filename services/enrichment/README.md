# enrichment

**Cohort:** Spring Boot
**Owned datastore:** — (small windowed state)

Normalize / dedup / deterministic-filter / trail-tag alarms for both history and live paths.

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/enrichment`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

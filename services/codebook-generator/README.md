# codebook-generator

**Cohort:** Python (networkx)
**Owned datastore:** PostgreSQL (codebook store)

Forward-propagation codebook: candidate root cause → predicted symptom signature, tagged to trails.

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/codebook-generator`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

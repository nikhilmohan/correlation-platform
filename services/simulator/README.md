# simulator

**Cohort:** Python (networkx)
**Owned datastore:** — (ground-truth labels in local/Postgres)

Generate a grounded synthetic Core IP topology + labeled alarm streams (history + live); the evaluation oracle.

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/simulator`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

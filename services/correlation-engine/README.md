# correlation-engine

**Cohort:** Spring Boot (Kafka Streams)
**Owned datastore:** PostgreSQL (Incident Store)

Real-time stateful match/score/RCA; one incident with root cause + children.

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/correlation-engine`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

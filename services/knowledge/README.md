# knowledge

**Cohort:** Spring Boot
**Owned datastore:** PostgreSQL (knowledge store)

Versioned domain knowledge: propagation templates, fault-origin types, trail policy, model params.

> Scaffold stub. See `spec.md` (contract) and `design.md` (how) for detail — both TBD until
> authored via the spec/design workflow. Build via the cohort dev agent on `build/knowledge`.

## Run
_TBD — added once the implementation lands. Standard: `/health` + `/metrics`, config from env,
Docker Compose entry._

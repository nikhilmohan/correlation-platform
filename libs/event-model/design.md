# event-model — Design

> **STATUS: TBD — first post-bootstrap work item (kickoff §11 step 1).**
> Scaffold only. To be written via `@designer` using the `design` skill after the spec is
> approved. Built first, then FROZEN.

<!-- To be completed: Stack (JSON Schema source of truth → Java binding + Python/Pydantic
     binding), module breakdown, envelope + payload schemas, (de)serialization + validation
     helpers, schemaVersion compatibility (reject unknown major), managedObjectId scheme,
     test plan mapping each acceptance criterion (incl. Java↔Python wire-format agreement)
     to a test, build/run. -->
